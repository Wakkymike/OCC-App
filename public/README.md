# Public Assets

This folder contains static assets for the application.

## How to add your logo:
1. Upload your image file to this directory.
2. Rename the file to `logo.png` (or update the filename in `src/lib/placeholder-images.json`).
3. The application will automatically serve this file from the root path (e.g., `/logo.png`).
