'use client';

/**
 * The map page is now an empty shell. The actual map UI lives in
 * <PersistentMapView /> which is rendered at the layout level so it
 * stays mounted (and hidden) when the user navigates away.
 */
export default function Page() {
  return null;
}
